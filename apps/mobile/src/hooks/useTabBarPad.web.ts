/** RNW-harness split of useTabBarPad: HomeTabs.web's JS tab bar (@react-navigation/bottom-tabs)
 *  takes normal layout space below the screens, so nothing overlays content — no pad needed. */
export function useTabBarPad(): number {
  return 0;
}
