// Ad chrome (src/ads/). The label is a policy requirement, not decoration: AdMob requires ads be
// clearly distinguishable from app content — the same wording apps/web's AdSlot renders.
export default {
  label: '廣告',
};
