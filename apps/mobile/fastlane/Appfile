app_identifier("tw.trmission.app")
# Apple Developer team id — only needed for local, interactive fastlane runs; CI authenticates with
# the App Store Connect API key instead, so these stay optional/env-driven.
apple_id(ENV["FASTLANE_APPLE_ID"]) if ENV["FASTLANE_APPLE_ID"]
team_id(ENV["APPLE_TEAM_ID"]) if ENV["APPLE_TEAM_ID"]
