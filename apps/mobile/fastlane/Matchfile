# Certificates + provisioning profiles live in a private git repo (MATCH_GIT_URL), encrypted with
# MATCH_PASSWORD. Run `fastlane match appstore` once locally to seed it; CI consumes it read-only.
git_url(ENV["MATCH_GIT_URL"])
storage_mode("git")
type("appstore")
app_identifier(["tw.trmission.app"])
