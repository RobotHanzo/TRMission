// Shared command surface (see @trm/client-core/src/net/commands.ts) — re-exported so the many
// component-level `from '../net/commands'` imports stay stable.
export type { EventPerkChoice, GameCommands } from '@trm/client-core';
